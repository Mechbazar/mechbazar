{
  "installCommand": "npm install",
  "buildCommand": "npm run build:web --workspace=rider",
  "outputDirectory": "apps/rider/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
