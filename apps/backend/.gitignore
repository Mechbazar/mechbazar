node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Real service-account credentials must never be committed
src/config/firebaseServiceAccount.json

.vercel
.env*
